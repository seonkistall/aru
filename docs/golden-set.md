# 골든셋 프로토콜 (회귀 측정용 고정 이미지 세트)

목적: 특징 추출/임계값/모델을 바꿀 때마다 **같은 입력에 대한 판독이 어떻게 변했는지** 측정한다.
골든셋 없이 코드를 바꾸면 개선인지 퇴행인지 알 수 없다.

## 구성 (20~30장)

| 축 | 값 |
|---|---|
| 인물 | 동의한 내부 인원 3~5명 (톤 다양성 확보: 밝은/중간/어두운 최소 1명씩) |
| 조명 | 창가 자연광 / 실내등 / 역광-근접 3종 |
| 거리 | 가이드 정위치 / 약간 멀리 2종 |
| 디바이스 | iPhone + Android 최소 1대씩 |
| 특이 케이스 | 메이크업 1명, 안경 1명 포함 |

## 수집 규칙

- `/scan?staff=1` + 학습 크롭 저장 동의 → 크롭 내보내기로 확보
- **레포에 절대 커밋하지 않는다** (얼굴 이미지). 로컬 `ml/data/golden/` (gitignore 대상) + 오프머신 백업 1곳
- 파일명: `golden-{인물ID}-{조명}-{거리}-{디바이스}.jpg`
- 라벨: 운영자 2인 합의(불일치 시 제외), `golden-labels.jsonl`로 관리

## 사용 방법

1. 특징/임계값 변경 전: 현재 코드로 골든셋 전체 판독 → 결과 저장
2. 변경 후: 다시 판독 → 레벨 변화 diff (몇 장이 어느 방향으로 움직였나)
3. 판정: 라벨 일치율이 오르면 채택, 내리면 롤백

## 하네스 (`/scan`이 아닌 `/eval`, 스태프 전용)

- [x] 이미지 업로드로 `analyzeSkin` 재판독(카메라 없이) — `/eval`
- [x] 판독 결과 JSONL 저장 + 이전 실행(baseline)과 레벨 diff
- [x] **정답 라벨 일치율(accuracy)** — `golden-labels.jsonl` 업로드 시 판독 vs 합의 라벨 일치%(전체+유분/붉은기/결별) 표시. 판정("라벨 일치율 오르면 채택")을 화면에서 바로 확인.

### `golden-labels.jsonl` 포맷 (한 줄 = 한 이미지)

```
{"file": "golden-p1-window-near-iphone.jpg", "oil": 1, "redness": 0, "pores": 2}
```

- `file`은 업로드하는 이미지 파일명과 정확히 일치해야 매칭됨. `oil`/`redness`/`pores`는 0/1/2 정수(운영자 2인 합의). 특정 속성 미확정 시 그 키를 빼면 해당 속성만 일치율에서 제외.

## 현재 상태

- [ ] 이미지 0장 — 실기기 QA 때 내부 인원으로 첫 10장 수집 권장
- 참고: 지금 판독 파이프라인 버전 `roi-calibrated-2026-07-03` (트리밍+톤 기록+gray-world 톤 보정)
